box: node

build:
  steps:
    - npm-install
    - grunt:
        tasks: compile

deploy:
  steps:
    - uetchy/gh-pages:
        token: $GIT_TOKEN
        repo: instagramas/natos.github.io
        domain: natos.github.io/instagramas
        path: public
